{
  "name": "masgarti-payroll",
  "private": true,
  "version": "1.0.0",
  "description": "Masgarti Payroll Processing System - Capacitor mobile wrapper",
  "scripts": {
    "build:web": "npm run build --prefix frontend",
    "cap:sync": "npm run build:web && npx cap sync",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2",
    "@capacitor/ios": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2",
    "typescript": "^5.9.2"
  }
}
